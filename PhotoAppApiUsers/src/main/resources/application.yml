server:
  port: ${PORT:0}

spring:
  application:
    name: users-ws
  devtools:
    restart:
      enabled: true
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true

  datasource:
    url: jdbc:mysql://localhost:3306/micro
    username: root
    password: 12345678

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8010/eureka
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}

gateway:
  ip: 192.168.20.25

token:
  expiration_time: 86400000
  secret: ${SECRET_KEY:KW&2N@T2q&njW'-(XBieYdR&KJ)1ZlEN>ZukA0nAtWmF+WJU#c0W6[ULgnYh<f+}

login:
  url:
    path: /users/login